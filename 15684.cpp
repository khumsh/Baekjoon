#include<bits/stdc++.h>
using namespace std;

int n, m, h, a[32][12];
vector<int> v[32];

int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	// input
	cin >> n >> m >> h;
	for (int i = 0; i < n; i++)
	{
		for (int j = 0; j < m; j++)
		{
			a[i][j]
		}
	}
	for (int i = 0; i < m; i++)
	{
		int p, q;
		cin >> p >> q;
		
		v[p].
	}
	
	
	
	
	return 0;
}
